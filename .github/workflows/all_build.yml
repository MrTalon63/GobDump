name: Build All

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

env:
    # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
    BUILD_TYPE: Release
    GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

permissions:
    contents: write

jobs:
    build_windows_msys2:
        runs-on: windows-2022

        defaults:
            run:
                shell: msys2 {0}

        steps:
            - uses: actions/checkout@v4

            - name: Set up MSYS2 UCRT64
              uses: msys2/setup-msys2@v2
              with:
                  msystem: UCRT64
                  update: true
                  install: base-devel git

            - name: Install dependencies
              run: ./windows/Setup-MSYS2.sh

            - name: Configure CMake
              run: |
                  cmake -S . -B build -G Ninja \
                      -DCMAKE_BUILD_TYPE=$BUILD_TYPE \
                      -DPLUGINS_ALL=ON \
                      -DBUILD_TESTING=OFF

            - name: Build
              run: cmake --build build --parallel

            - name: Package portable
              run: |
                  cmake --install build --prefix install
                  cp -r resources pipelines install/ 2>/dev/null || true

            - name: Upload Portable
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Windows_MSYS2_UCRT64_Portable
                  path: install

    build_windows_x64:
        runs-on: windows-2022

        steps:
            - uses: actions/checkout@v4

            - name: Cache dependencies
              uses: actions/cache@v4
              id: win-deps-x64
              with:
                  path: vcpkg
                  key: win-deps-x64-${{ hashFiles('windows/Configure-vcpkg.ps1') }}

            - name: Set up Python 3.13
              if: steps.win-deps-x64.outputs.cache-hit != 'true'
              uses: actions/setup-python@v5
              with:
                  python-version: 3.13

            - name: Install mako
              if: steps.win-deps-x64.outputs.cache-hit != 'true'
              run: pip install mako

            - name: Add msbuild to PATH
              if: steps.win-deps-x64.outputs.cache-hit != 'true'
              uses: microsoft/setup-msbuild@v2

            - name: Configure vcpkg
              if: steps.win-deps-x64.outputs.cache-hit != 'true'
              shell: powershell
              run: ${{github.workspace}}\windows\Configure-vcpkg.ps1 -platform x64-windows

            - name: Clean up vcpkg
              if: steps.win-deps-x64.outputs.cache-hit != 'true'
              run: rm -recurse -force $Env:GITHUB_WORKSPACE/vcpkg/packages, $Env:GITHUB_WORKSPACE/vcpkg/downloads, $Env:GITHUB_WORKSPACE/vcpkg/buildtrees

# I FUCKING HATE AARONIA
#            - name: Download Aaronia RTSA API
#              if: steps.win-deps-x64.outputs.cache-hit != 'true'
#              shell: powershell
#              run: Invoke-WebRequest -Uri "https://www.satdump.org/Aaronia_RTSA_Suite_PRO_2.2.0.12821_Setup_64bit.exe" -OutFile $Env:GITHUB_WORKSPACE/vcpkg/rtsa.exe
#
#            - name: Install Aaronia RTSA API
#              shell: cmd
#              run: start /wait vcpkg\rtsa.exe /q2 /install

            - name: Add support to NSIS for long PATHs
              shell: powershell
              run: ${{github.workspace}}\windows\Get-NSIS-LongStrings.ps1

            - name: Create Build Environment
              run: cmake -E make_directory ${{runner.workspace}}/build

            - name: Configure CMake
              working-directory: ${{runner.workspace}}/build
              run: cmake -DVCPKG_TARGET_TRIPLET="x64-windows" -DPLUGIN_AARONIA_SDR_SUPPORT=ON -DBUILD_MSVC=ON -DCMAKE_TOOLCHAIN_FILE="${{github.workspace}}\vcpkg\scripts\buildsystems\vcpkg.cmake" "$Env:GITHUB_WORKSPACE"

            - name: Build
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: cmake --build . --config $BUILD_TYPE

            - name: Build Installer
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: cpack -C $BUILD_TYPE

            - name: Upload Installer
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Windows_x64_Installer
                  path: ${{runner.workspace}}/build/GobDump-*.exe

            - name: Add dumpbin to PATH
              run: echo "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Tools\MSVC\$((Get-ChildItem -Path "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Tools\MSVC" | Sort-Object Name -Descending | Select-Object -First 1).Name)\bin\Hostx64\x64" >> $env:GITHUB_PATH

            - name: Build Portable
              shell: powershell
              run: ${{github.workspace}}\windows\Finish-Release.ps1 -platform x64-windows -BuildPath ${{runner.workspace}}\build

            - name: Upload Portable
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Windows_x64_Portable
                  path: ${{runner.workspace}}/build/Release/*

    build_windows_arm64:
        runs-on: windows-2022

        steps:
            - uses: actions/checkout@v4

            - name: Cache dependencies
              uses: actions/cache@v4
              id: win-deps-arm64
              with:
                  path: vcpkg
                  key: win-deps-arm64-${{ hashFiles('windows/Configure-vcpkg.ps1') }}

            - name: Set up Python 3.13
              if: steps.win-deps-arm64.outputs.cache-hit != 'true'
              uses: actions/setup-python@v5
              with:
                  python-version: 3.13

            - name: Install mako
              if: steps.win-deps-arm64.outputs.cache-hit != 'true'
              run: pip install mako

            - name: Add msbuild to PATH
              if: steps.win-deps-arm64.outputs.cache-hit != 'true'
              uses: microsoft/setup-msbuild@v2

            - name: Configure vcpkg
              if: steps.win-deps-arm64.outputs.cache-hit != 'true'
              shell: powershell
              run: ${{github.workspace}}\windows\Configure-vcpkg.ps1 -platform arm64-windows

            - name: Clean up vcpkg
              if: steps.win-deps-arm64.outputs.cache-hit != 'true'
              run: rm -recurse -force $Env:GITHUB_WORKSPACE/vcpkg/packages, $Env:GITHUB_WORKSPACE/vcpkg/downloads, $Env:GITHUB_WORKSPACE/vcpkg/buildtrees

            - name: Add support to NSIS for long PATHs
              shell: powershell
              run: ${{github.workspace}}\windows\Get-NSIS-LongStrings.ps1

            - name: Create Build Environment
              run: cmake -E make_directory ${{runner.workspace}}/build

            - name: Configure CMake
              working-directory: ${{runner.workspace}}/build
              run: cmake -A ARM64 -DVCPKG_TARGET_TRIPLET="arm64-windows" -DVCPKG_HOST_TRIPLET="x64-windows" -DVCPKG_USE_HOST_TOOLS=ON -DCMAKE_SYSTEM_VERSION="10.0.22621.0" -DCMAKE_SYSTEM_NAME=Windows -DCMAKE_SYSTEM_PROCESSOR=ARM64 -DCMAKE_CROSSCOMPILING=ON -DPLUGIN_LIMESDR_SDR_SUPPORT=OFF -DBUILD_MSVC=ON -DCMAKE_TOOLCHAIN_FILE="${{github.workspace}}\vcpkg\scripts\buildsystems\vcpkg.cmake" "$Env:GITHUB_WORKSPACE"

            - name: Build
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: cmake --build . --config $BUILD_TYPE

            - name: Build Installer
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: cpack -C $BUILD_TYPE

            - name: Upload Installer
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Windows_arm64_Installer
                  path: ${{runner.workspace}}/build/GobDump-*.exe

            - name: Add dumpbin to PATH
              run: echo "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Tools\MSVC\$((Get-ChildItem -Path "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Tools\MSVC" | Sort-Object Name -Descending | Select-Object -First 1).Name)\bin\Hostx64\x64" >> $env:GITHUB_PATH

            - name: Build Portable
              shell: powershell
              run: ${{github.workspace}}\windows\Finish-Release.ps1 -platform arm64-windows -BuildPath ${{runner.workspace}}\build

            - name: Upload Portable
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Windows_arm64_Portable
                  path: ${{runner.workspace}}/build/Release/*

    build_android:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
              with:
                  submodules: true

            - name: Set up JDK
              uses: actions/setup-java@v5
              with:
                java-version: "17"
                distribution: "temurin"

            - name: Set up Android SDK
              uses: android-actions/setup-android@v3

            - name: Install NDK, platform, and CMake
              shell: bash
              run: |
                  sdkmanager --install "ndk;25.2.9519653" "platforms;android-34" "cmake;3.22.1"

            - name: Cache dependencies
              uses: actions/cache@v4
              id: android-deps
              with:
                  path: android/deps/output
                  key: android-deps-v3-${{ hashFiles('android/deps/build.sh') }}

            - name: Install dependency build tools
              if: steps.android-deps.outputs.cache-hit != 'true'
              run: sudo apt-get install -y python3-mako

            - name: Build dependencies
              if: steps.android-deps.outputs.cache-hit != 'true'
              shell: bash
              run: cd $GITHUB_WORKSPACE/android/deps && sh build.sh

            - name: Generate Gradle wrapper
              uses: gradle/actions/setup-gradle@v4
              with:
                  gradle-version: 8.10.2

            - name: Build
              shell: bash
              run: |
                  # Generate the wrapper in a clean dir to avoid evaluating the project's AGP
                  # Gradle 8.10.2 requires a settings file to exist before running 'gradle wrapper'
                  mkdir -p /tmp/gradle-wrapper && cd /tmp/gradle-wrapper
                  echo "" > settings.gradle
                  gradle wrapper --gradle-version 8.10.2 --distribution-type bin
                  cp -r gradlew gradlew.bat gradle $GITHUB_WORKSPACE/android/
                  cd $GITHUB_WORKSPACE/android
                  ./gradlew assembleRelease

            - name: Sign APK
              shell: bash
              run: |
                  APK=$GITHUB_WORKSPACE/android/app/build/outputs/apk/release/app-release-unsigned.apk
                  SIGNED=$GITHUB_WORKSPACE/android/app/build/outputs/apk/release/app-release-signed.apk
                  echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > /tmp/release.keystore
                  $ANDROID_HOME/build-tools/30.0.3/apksigner sign \
                      --ks /tmp/release.keystore \
                      --ks-key-alias ${{ secrets.ANDROID_KEY_ALIAS }} \
                      --ks-pass pass:${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
                      --key-pass pass:${{ secrets.ANDROID_KEY_PASSWORD }} \
                      --out $SIGNED \
                      $APK

            - name: Upload APK
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-release.apk
                  path: ${{github.workspace}}/android/app/build/outputs/apk/release/app-release-signed.apk

    build_ubuntu_2204:
        # The CMake configure and build commands are platform agnostic and should work equally
        # well on Windows or Mac.  You can convert this to a matrix build if you need
        # cross-platform coverage.
        # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
        runs-on: ubuntu-22.04

        steps:
            - uses: actions/checkout@v4

            - name: Update repositories
              run: sudo apt update

            - name: Install dependencies
              run: sudo apt install -y libdbus-1-dev libcurl4-openssl-dev libglfw3-dev libfftw3-dev libvolk2-dev build-essential cmake pkgconf libjpeg-dev libpng-dev libtiff-dev libairspy-dev libairspyhf-dev libhackrf-dev librtlsdr-dev libomp-dev libnng-dev libiio-dev libzstd-dev libad9361-dev libbladerf-dev libuhd-dev liblimesuite-dev ocl-icd-opencl-dev libjemalloc-dev portaudio19-dev libhdf5-dev ninja-build

            - name: Create Build Environment
              # Some projects don't allow in-source building, so create a separate build directory
              # We'll use this as our working directory for all subsequent commands
              run: cmake -E make_directory ${{runner.workspace}}/build

            - name: Configure CMake
              # Use a bash shell so we can use the same syntax for environment variable
              # access regardless of the host operating system
              shell: bash
              working-directory: ${{runner.workspace}}/build
              # Note the current convention is to use the -S and -B options here to specify source
              # and build directories, but this is only available with CMake 3.13 and higher.
              # The CMake binaries on the Github Actions machines are (as of this writing) 3.12
              run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DCMAKE_INSTALL_PREFIX=/usr -G Ninja

            - name: Build
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja

            - name: Package
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja package

            - name: Upload ZIP
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Ubuntu-22.04
                  path: ${{runner.workspace}}/build/*.deb

    build_ubuntu_2404:
        # The CMake configure and build commands are platform agnostic and should work equally
        # well on Windows or Mac.  You can convert this to a matrix build if you need
        # cross-platform coverage.
        # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
        runs-on: ubuntu-24.04

        steps:
            - uses: actions/checkout@v4

            - name: Update repositories
              run: sudo apt update

            - name: Install dependencies
              run: sudo apt install -y libdbus-1-dev libcurl4-openssl-dev libglfw3-dev libfftw3-dev libvolk-dev build-essential cmake pkgconf libjpeg-dev libpng-dev libtiff-dev libairspy-dev libairspyhf-dev libhackrf-dev librtlsdr-dev libomp-dev libnng-dev libiio-dev libzstd-dev libad9361-dev libbladerf-dev libuhd-dev liblimesuite-dev ocl-icd-opencl-dev libjemalloc-dev portaudio19-dev libhdf5-dev ninja-build

            - name: Create Build Environment
              # Some projects don't allow in-source building, so create a separate build directory
              # We'll use this as our working directory for all subsequent commands
              run: cmake -E make_directory ${{runner.workspace}}/build

            - name: Configure CMake
              # Use a bash shell so we can use the same syntax for environment variable
              # access regardless of the host operating system
              shell: bash
              working-directory: ${{runner.workspace}}/build
              # Note the current convention is to use the -S and -B options here to specify source
              # and build directories, but this is only available with CMake 3.13 and higher.
              # The CMake binaries on the Github Actions machines are (as of this writing) 3.12
              run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DCMAKE_INSTALL_PREFIX=/usr -G Ninja

            - name: Build
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja

            - name: Package
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja package

            - name: Upload ZIP
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Ubuntu-24.04
                  path: ${{runner.workspace}}/build/*.deb

    build_linux_arm64:
        # The CMake configure and build commands are platform agnostic and should work equally
        # well on Windows or Mac.  You can convert this to a matrix build if you need
        # cross-platform coverage.
        # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
        runs-on: ubuntu-22.04-arm

        steps:
            - uses: actions/checkout@v4

            - name: Update repositories
              run: sudo apt update

            - name: Install dependencies
              run: sudo apt install -y libdbus-1-dev libcurl4-openssl-dev libglfw3-dev libfftw3-dev libvolk2-dev build-essential cmake pkgconf libjpeg-dev libpng-dev libtiff-dev libairspy-dev libairspyhf-dev libhackrf-dev librtlsdr-dev libomp-dev libnng-dev libiio-dev libzstd-dev libad9361-dev libbladerf-dev libuhd-dev liblimesuite-dev ocl-icd-opencl-dev libjemalloc-dev portaudio19-dev libhdf5-dev libsqlite3-dev ninja-build

            - name: Delete build dir
              run: rm -rf ${{runner.workspace}}/build

            - name: Create Build Environment
              # Some projects don't allow in-source building, so create a separate build directory
              # We'll use this as our working directory for all subsequent commands
              run: cmake -E make_directory ${{runner.workspace}}/build

            - name: Configure CMake
              # Use a bash shell so we can use the same syntax for environment variable
              # access regardless of the host operating system
              shell: bash
              working-directory: ${{runner.workspace}}/build
              # Note the current convention is to use the -S and -B options here to specify source
              # and build directories, but this is only available with CMake 3.13 and higher.
              # The CMake binaries on the Github Actions machines are (as of this writing) 3.12
              run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE -G Ninja

            - name: Build
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja

            - name: Package
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja package

            - name: Upload ZIP
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-Pi64-Latest
                  path: ${{runner.workspace}}/build/*.deb

    build_macos_intel:
        runs-on: macos-15-intel

        steps:
            - uses: actions/checkout@v4

            - name: Select Xcode version
              run: sudo xcode-select -s '/Applications/Xcode_16.2.app/Contents/Developer'

            - name: Install brew dependencies
              # These are preinstalled in the GitHub runners, cause needless warnings to appear
              # Add these back if these ever become missing for some reason
              # sqlite3 libtool autoconf libjpeg-turbo libtiff libusb libpng zstd
              run: brew install ninja dylibbundler automake meson orc libxml2 zlib portaudio boost nng volk libomp glfw3 fftw libxml2 portaudio jemalloc nng armadillo hdf5 protobuf grpc

            - name: Create Build Environment
              # Some projects don't allow in-source building, so create a separate build directory
              # We'll use this as our working directory for all subsequent commands
              run: cmake -E make_directory ${{runner.workspace}}/build

            - name: Cache dependencies
              uses: actions/cache@v4
              id: macos-deps-intel
              with:
                  path: deps
                  key: macos-deps-intel-${{ hashFiles('macOS/build_deps.sh') }}

            - name: Build dependencies
              if: steps.macos-deps-intel.outputs.cache-hit != 'true'
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: $GITHUB_WORKSPACE/macOS/build_deps.sh

            - name: Configure CMake
              working-directory: ${{runner.workspace}}/build
              # Note the current convention is to use the -S and -B options here to specify source
              # and build directories, but this is only available with CMake 3.13 and higher.
              # The CMake binaries on the Github Actions machines are (as of this writing) 3.12
              run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DBUNDLING_MODE=ON -DCMAKE_OSX_DEPLOYMENT_TARGET=11.0 -G Ninja

            - name: Build
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja

            - name: Bundle
              working-directory: ${{runner.workspace}}/build
              shell: bash
              #env:
              #  MACOS_TEAM: ${{ secrets.MACOS_TEAM }}
              #  MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
              #  MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
              #  MACOS_SIGNING_SIGNATURE: ${{ secrets.MACOS_SIGNING_SIGNATURE }}
              #  MACOS_NOTARIZATION_UN: ${{ secrets.MACOS_NOTARIZATION_UN }}
              #  MACOS_NOTARIZATION_PWD: ${{ secrets.MACOS_NOTARIZATION_PWD }}
              run: $GITHUB_WORKSPACE/macOS/bundle.sh

            - name: Upload DMG
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-macOS-Intel
                  path: ${{runner.workspace}}/build/*.dmg

    build_macos_silicon:
        runs-on: macos-15
        steps:
            - uses: actions/checkout@v4

            - name: Select Xcode version
              run: sudo xcode-select -s '/Applications/Xcode_16.2.app/Contents/Developer'

            - name: Install brew dependencies
              # These are preinstalled in the GitHub runners, cause needless warnings to appear
              # Add these back if these ever become missing for some reason
              run: brew install ninja dylibbundler libtool autoconf automake meson orc libxml2 zlib portaudio boost nng volk libomp glfw3 fftw libxml2 portaudio jemalloc nng armadillo hdf5 protobuf grpc

            - name: Create Build Environment
              # Some projects don't allow in-source building, so create a separate build directory
              # We'll use this as our working directory for all subsequent commands
              run: cmake -E make_directory ${{runner.workspace}}/build

            - name: Cache dependencies
              uses: actions/cache@v4
              id: macos-deps-silicon
              with:
                  path: deps
                  key: macos-deps-silicon-${{ hashFiles('macOS/build_deps.sh') }}

            - name: Build dependencies
              if: steps.macos-deps-silicon.outputs.cache-hit != 'true'
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: $GITHUB_WORKSPACE/macOS/build_deps.sh

            - name: Configure CMake
              working-directory: ${{runner.workspace}}/build
              # Note the current convention is to use the -S and -B options here to specify source
              # and build directories, but this is only available with CMake 3.13 and higher.
              # The CMake binaries on the Github Actions machines are (as of this writing) 3.12
              run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DBUNDLING_MODE=ON -DCMAKE_OSX_DEPLOYMENT_TARGET=11.0 -G Ninja

            - name: Build
              working-directory: ${{runner.workspace}}/build
              shell: bash
              run: ninja

            - name: Bundle
              working-directory: ${{runner.workspace}}/build
              shell: bash
              #env:
              #  MACOS_TEAM: ${{ secrets.MACOS_TEAM }}
              #  MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
              #  MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
              #  MACOS_SIGNING_SIGNATURE: ${{ secrets.MACOS_SIGNING_SIGNATURE }}
              #  MACOS_NOTARIZATION_UN: ${{ secrets.MACOS_NOTARIZATION_UN }}
              #  MACOS_NOTARIZATION_PWD: ${{ secrets.MACOS_NOTARIZATION_PWD }}
              run: $GITHUB_WORKSPACE/macOS/bundle.sh

            - name: Upload DMG
              uses: actions/upload-artifact@v4
              with:
                  name: GobDump-macOS-Silicon
                  path: ${{runner.workspace}}/build/*.dmg

    # Yes, since I was asked to do like SDR++ for this I did look at how Ryzerth has done it :-)
    update_nightly_release:
        needs: ["build_windows_x64", "build_windows_arm64", "build_ubuntu_2204", "build_ubuntu_2404", "build_macos_intel", "build_macos_silicon", "build_linux_arm64", "build_android"]
        runs-on: ubuntu-latest
        if: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/master' }}

        steps:
            - name: Download Artifacts
              uses: actions/download-artifact@v4

            - name: Install zip
              run: sudo apt install -y unzip

            - name: Copy files
              run: mkdir gobdump_out && cp GobDump-Ubuntu-22.04/gobdump*.deb gobdump_out/gobdump_ubuntu_22.04_amd64.deb && cp GobDump-Ubuntu-24.04/gobdump*.deb gobdump_out/gobdump_ubuntu_24.04_amd64.deb && cp GobDump-macOS-Intel/GobDump-macOS-Intel.dmg gobdump_out/GobDump-macOS-Intel.dmg && cp GobDump-macOS-Silicon/GobDump-macOS-Silicon.dmg gobdump_out/GobDump-macOS-Silicon.dmg && (cd GobDump-Windows_x64_Portable && zip -r ../gobdump_out/GobDump-Windows_x64_Portable.zip *) && cp GobDump-Windows_x64_Installer/GobDump-*.exe gobdump_out/GobDump-Windows_x64_Installer.exe && cp GobDump-Pi64-Latest/gobdump*.deb gobdump_out/gobdump_rpi64_latest_arm64.deb && (cd GobDump-Windows_arm64_Portable && zip -r ../gobdump_out/GobDump-Windows_arm64_Portable.zip *) && cp GobDump-Windows_arm64_Installer/GobDump-*.exe gobdump_out/GobDump-Windows_arm64_Installer.exe && cp GobDump-release.apk/app-release-signed.apk gobdump_out/GobDump-Android.apk

            - name: Update Nightly
              run: gh release upload nightly gobdump_out/* -R ${{github.repository}} --clobber
